<!DOCTYPE html>
<html>
    <head>
        <title>Yelpcamp App</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/main.css">   
    </head>
    <body>
      <!--navbar-->
      <nav class="navbar navbar-expand-md navbar-dark bg-dark mb-3">
        <!--<div class="container">-->
            <a class="navbar-brand" href="/"> YELPCAMP</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#toggledContent"   aria="toggler">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse justify-content-end " id="toggledContent">
                <ul class="navbar-nav ">
                <% if(!currentUser){ %>
                        <li class="nav-item">
                           <a class="nav-link" href="/login">Login</a> 
                        <li>
                        <li class="nav-item">
                           <a class="nav-link" href="/register">Signup</a> 
                        <li>
                  <%  }else{  %>
                        <li class="nav-item">
                           <a class="nav-link" href="#">Logged in as <%= currentUser.username.toUpperCase() %></a> 
                        <li>
                        <li class="nav-item ">
                           <a class="nav-link" href="/logout">Logout</a> 
                        <li>
                 <%  } %>               
                <ul>    
            </div>
        <!--</div>-->
      </nav>
      
      <div class="container">
         <% if(error && error.length >0){ %>
               <div class="alert alert-danger" role="alert">
                <%= error %>
               </div>
         <% } %>
          
         <% if(success && success.length >0){ %>
               <div class="alert alert-success" role="alert">
                <%= success %>
               </div>
         <% } %>
      </div>
      
   